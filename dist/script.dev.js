"use strict";

varfunction = getPizza(name, size, topping, crust, total);
{
  (void 0).name = name;
  (void 0).size = size;
  (void 0).topping = topping;
  This.crust = crust;
  (void 0).total = total;
}